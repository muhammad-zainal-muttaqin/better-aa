# Base URL of the deployed Worker that serves /api/models.
# Local dev (wrangler dev): http://localhost:8787
# Production: https://better-aa-worker.<your-subdomain>.workers.dev
PUBLIC_API_BASE=http://localhost:8787

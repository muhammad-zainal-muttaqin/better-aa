{
  "name": "better-aa-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Daily ingest of Artificial Analysis model data into Workers KV + read API",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240605.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
